(*

caution:

while uing the REPL:
  do not use "use" more than once for the same file
  without re-starting the REPL, 
  Because, you'll be repeating whatever bindings were there for the first time.
  It's legal, but might cause issues such as confusion
  
  *)


(* error checking:

  - syntax error
  - type checking error
  - evaluation
  
 *)
